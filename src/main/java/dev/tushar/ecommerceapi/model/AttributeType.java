package dev.tushar.ecommerceapi.model;

public enum AttributeType {
    TEXT,   // For free text, like a color hex code or specific measurement
    ENUM,   // For a predefined list of options, like size
    BOOLEAN // For a simple true/false value
}